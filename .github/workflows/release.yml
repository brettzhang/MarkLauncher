name: Release Chrome Extension

on:
  push:
    tags:
      - 'v*'  # 只有推送 v* tag 才触发

permissions:
  contents: write  # 需要写 Release

jobs:
  build-and-release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Get version from tag
        id: get_version
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

      - name: Create extension zip
        run: |
          cd extension
          zip -r ../marklauncher-v${{ steps.get_version.outputs.VERSION }}.zip . -x "*.DS_Store" "*.git*"

      - name: Generate release notes
        id: release_notes
        run: |
          echo "## 🚀 MarkLauncher ${{ steps.get_version.outputs.VERSION }}" > release_notes.md
          echo "" >> release_notes.md
          echo "### 📦 下载说明" >> release_notes.md
          echo "1. 下载 \`marklauncher-v${{ steps.get_version.outputs.VERSION }}.zip\`" >> release_notes.md
          echo "2. 解压 zip 文件" >> release_notes.md
          echo "3. 打开 Chrome 浏览器，访问 \`chrome://extensions/\`" >> release_notes.md
          echo "4. 启用\"开发者模式\"" >> release_notes.md
          echo "5. 点击\"加载已解压的扩展程序\"，选择解压后的文件夹" >> release_notes.md
          echo "" >> release_notes.md
          echo "### ✨ 更新内容" >> release_notes.md
          echo "请查看 [提交记录](https://github.com/${{ github.repository }}/commits/v${{ steps.get_version.outputs.VERSION }}) 了解详细更新内容。" >> release_notes.md

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: marklauncher-v${{ steps.get_version.outputs.VERSION }}.zip
          body_path: release_notes.md
          draft: false
          prerelease: false
          generate_release_notes: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
